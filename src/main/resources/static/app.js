const form=document.getElementById('test-form');const startButton=document.getElementById('start');const cancelButton=document.getElementById('cancel');const message=document.getElementById('form-message');const panel=document.getElementById('progress-panel');const canvas=document.getElementById('speed-chart');const context=canvas.getContext('2d');let currentTestId=null;let eventSource=null;let speedSamples=[];
function requestPayload(){return{endpoint:document.getElementById('endpoint').value.trim(),bucket:document.getElementById('bucket').value.trim(),region:document.getElementById('region').value.trim(),accessKey:document.getElementById('accessKey').value,secretKey:document.getElementById('secretKey').value,pathStyleAccess:document.getElementById('pathStyleAccess').checked,objectKey:document.getElementById('objectKey').value.trim(),objectSizeMiB:Number(document.getElementById('objectSizeMiB').value),partSizeMiB:Number(document.getElementById('partSizeMiB').value),parallelism:Number(document.getElementById('parallelism').value),objectCount:Number(document.getElementById('objectCount').value),deleteAfterTest:document.getElementById('deleteAfterTest').checked,operation:document.getElementById('operation').value};}
async function jsonRequest(url,options){const response=await fetch(url,options);if(!response.ok){throw new Error((await response.text())||`HTTP ${response.status}`);}return response.json();}
form.addEventListener('submit',async event=>{event.preventDefault();startButton.disabled=true;message.textContent='Создание теста…';speedSamples=[];drawChart();try{const run=await jsonRequest('/api/tests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(requestPayload())});currentTestId=run.id;panel.hidden=false;cancelButton.disabled=false;document.getElementById('reports').hidden=true;subscribe(run.id);updateProgress(run);}catch(error){message.textContent=error.message;startButton.disabled=false;}});
document.getElementById('load-buckets').addEventListener('click',async()=>{message.textContent='Получение списка бакетов…';try{const buckets=await jsonRequest('/api/buckets',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(requestPayload())});if(!buckets.length)message.textContent='Доступных бакетов не найдено';else{document.getElementById('bucket').value=buckets[0];message.textContent=`Найдено бакетов: ${buckets.length}. Выбран ${buckets[0]}`;}}catch(error){message.textContent=error.message;}});
cancelButton.addEventListener('click',async()=>{if(!currentTestId)return;try{updateProgress(await jsonRequest(`/api/tests/${currentTestId}/cancel`,{method:'POST'}));}catch(error){message.textContent=error.message;}});
function subscribe(id){if(eventSource)eventSource.close();eventSource=new EventSource(`/api/tests/${id}/events`);eventSource.addEventListener('progress',event=>{const run=JSON.parse(event.data);updateProgress(run);if(['COMPLETED','FAILED','CANCELLED'].includes(run.status))finishRun(run);});eventSource.onerror=()=>eventSource&&eventSource.close();}
function updateProgress(run){document.getElementById('status').textContent=`${run.operation} · ${run.status}`;document.getElementById('percent').textContent=`${run.percent.toFixed(1)}%`;document.getElementById('bytes').textContent=run.totalBytes>0?`${(run.bytesTransferred/1048576).toFixed(1)} / ${(run.totalBytes/1048576).toFixed(1)} MiB`:`операции ${run.completedParts}/${run.totalParts}`;document.getElementById('current-speed').textContent=`${run.currentSpeedMiBps.toFixed(2)} MiB/s`;document.getElementById('average-speed').textContent=`${run.averageSpeedMiBps.toFixed(2)} MiB/s`;document.getElementById('latency').textContent=`${run.p50LatencyMs.toFixed(0)} / ${run.p95LatencyMs.toFixed(0)} / ${run.p99LatencyMs.toFixed(0)} ms`;document.getElementById('errors').textContent=run.failedParts;document.getElementById('progress-bar').style.width=`${run.percent}%`;document.getElementById('progress-message').textContent=`${run.message} · операции ${run.completedParts}/${run.totalParts}`;if(run.status==='RUNNING'&&run.currentSpeedMiBps>0){speedSamples.push(run.currentSpeedMiBps);if(speedSamples.length>60)speedSamples.shift();drawChart();}}
function finishRun(run){startButton.disabled=false;cancelButton.disabled=true;message.textContent=run.status==='COMPLETED'?'Тест завершён':run.message;if(eventSource)eventSource.close();document.getElementById('reports').hidden=false;document.getElementById('json-report').href=`/api/tests/${run.id}/report.json`;document.getElementById('csv-report').href=`/api/tests/${run.id}/report.csv`;refreshHistory();}
async function refreshHistory(){try{const runs=await jsonRequest('/api/tests');document.getElementById('history').innerHTML=runs.map(run=>`<tr><td>${new Date(run.createdAt).toLocaleString()}</td><td>${escapeHtml(run.operation)}</td><td>${escapeHtml(run.endpoint)} / ${escapeHtml(run.bucket)}</td><td>${run.status}</td><td>${(run.totalBytes/1048576).toFixed(1)} MiB</td><td>${run.averageSpeedMiBps.toFixed(2)} MiB/s</td><td>${run.p95LatencyMs.toFixed(0)} ms</td></tr>`).join('');}catch(_){}}
function drawChart(){const width=canvas.width,height=canvas.height;context.clearRect(0,0,width,height);context.strokeStyle='#dfe5eb';context.lineWidth=1;for(let i=1;i<5;i++){const y=i*height/5;context.beginPath();context.moveTo(0,y);context.lineTo(width,y);context.stroke();}if(speedSamples.length<2)return;const max=Math.max(...speedSamples,1);context.strokeStyle='#1769cf';context.lineWidth=4;context.beginPath();speedSamples.forEach((value,index)=>{const x=index*width/(speedSamples.length-1);const y=height-(value/max)*(height-20)-10;if(index===0)context.moveTo(x,y);else context.lineTo(x,y);});context.stroke();}
function escapeHtml(value){return String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));}
